# Lekka Patra (ಲೆಕ್ಕ ಪತ್ರ) — Farm Labor Tracker (PRD)

## Original Problem Statement
Production-ready, mobile-responsive web app for agriculturists to track field labor attendance, daily wages, advance payments and daily work logs. Bilingual (Kannada/English), Google + Phone OTP sign-in, cloud DB, 15-day trial then ₹99/yr Razorpay subscription (read-only if unpaid), workers & contractors, ledger with Mark Settled cutoff, PDF/Excel + WhatsApp reports, hidden Owner Portal with ad management, analytics, feedback.

## Architecture
- Backend: FastAPI + MongoDB (motor)
- Frontend: React 19 + Tailwind + Shadcn UI + Recharts
- Auth: Emergent-managed Google OAuth + Dummy OTP (Firebase paused)
- Payments: Razorpay ₹99/yr subscription
- Reports: reportlab (PDF) + openpyxl (Excel)
- PWA: manifest.json + custom icons (square 16→1024, maskable 192/512/1024)

## Implemented
- Bilingual (EN/KN) with i18n dictionary
- Google Sign-In + Dummy OTP login
- Workers CRUD, Attendance (4 statuses), Advances/Settlements
- Contractor visits, payments, returns, per-contractor ledger
- Mark Settled = cycle cutoff (resets pending wages & days_worked, keeps history)
- Dashboard with sliding marquee + Ad Carousel (photo-only, swipe, 10s auto)
- PDF (detailed date-wise) + Excel exports, WhatsApp share
- Owner Portal (RBAC via OWNER_MOBILE/OWNER_EMAIL): user & location analytics, ad manager, feedback inbox
- 15-day trial + ₹99/yr Razorpay subscription with read-only lock (HTTP 402)
- App rebrand to "Lekka Patra" everywhere
- **[2026-02-11] Custom App Icon & PWA:** Square-cropped Lekka Patra logo installed as favicon.ico + PNG icons (16, 32, 48, 64, 96, 128, 144, 152, 180, 192, 256, 384, 512, 1024) + maskable variants (192/512/1024) with green safe-zone padding. manifest.json updated with 15 icons and theme/background `#1c683b`. index.html adds full apple-touch-icon set, mask-icon, and multiple icon size links for Android + iOS home-screen installs and PWA splash.
- **[2026-02-11] Bubblewrap TWA scaffolding:** `/app/mobile/twa-manifest.json` + `/app/mobile/README.md` step-by-step APK build guide. `/.well-known/assetlinks.json` served live on the PWA domain for Digital Asset Links verification (fingerprint placeholder to be filled after user runs `bubblewrap init`).
- **[2026-02-12] Worker Type & Attendance grouping:** Added `worker_type` field (regular | temporary, default regular) to Worker model + WorkerIn. Workers form now has a segmented Regular/Temporary toggle. Worker list rows show a colored badge. Attendance page groups workers into two sticky sections — **Regular Workers** first, then **Temporary Workers** — each with a header and count. Sections only render when non-empty. Bilingual (EN/KN) labels added.
- **[2026-02-12] Global Beta Marquee:** New `BetaBanner` component mounted at app root (App.js) — fixed at top, `#FFF9C4` background, `#333333` text, 30px tall, `z-index: 60`, seamless continuous scroll (42s loop, dual-copy track). Bilingual Kannada + English text: "ಗಮನಿಸಿ: ಈ ಅಪ್ಲಿಕೇಶನ್ ಪರೀಕ್ಷಾ ಹಂತದಲ್ಲಿದೆ… / Note: This app is in beta phase. Please maintain a manual backup of your important data." Sets `--beta-banner-h` CSS var so sticky page headers offset correctly. Visible on every screen including Login, Dashboard, Attendance, Workers, Ledger, Settings, Paywall, Owner Portal. Respects `prefers-reduced-motion`.
- **[2026-02-13] Final Balance accounting fix (v1):** Added `final_balance` field to `compute_worker_ledger`. Initial formula was `total_earned - net_advance - total_settled` (proven buggy in v2 audit).
- **[2026-02-13] Full accounting + data-integrity audit v3:** After user re-audit, discovered 4 additional issues and fixed them systematically:
- **[2026-02-13] Historical wage lock (Attendance.daily_rate_snapshot):** ROOT CAUSE for the "editing worker wage rewrites past earnings" bug: `Attendance` did not store the wage in force on the day it was recorded — every ledger read computed `_wage_units(status, ot, worker["daily_rate"])` with the *current* rate. Fix: added optional `daily_rate_snapshot: Optional[float] = None` to the Attendance model and `POST /api/attendance` (routes/attendance.py) now snapshots `worker.daily_rate` at insert time. Existing rows (no snapshot) unchanged — computed with a fallback to the worker's current rate for backward compat. `services/ledger.py::compute_worker_ledger` and `routes/dashboard.py` now prefer `a["daily_rate_snapshot"]` over `worker["daily_rate"]`. No migration performed. No historical rows modified. 7 new audit tests (33–39): 500→600, 500→600→700, wage change with no new work (total unchanged), legacy fallback, settle across wage change, cross-user isolation on wage change, and a live HTTP e2e test that hits `POST /workers`, `POST /attendance`, `PUT /workers` with a wage bump, and `GET /ledger/{id}` — confirms `total_earned = ₹4,400` (NOT ₹4,800). 40/40 tests pass.
- **[2026-02-13] Contractor ledger additive fix:** Contractor accounting was correct but incomplete — `compute_contractor_ledger` didn't expose `total_settled` or `final_balance`, so history views couldn't distinguish "return via settlement" from "voluntary return" and the UI/exports lacked a sign-aware direction label. Fixed (READ-SIDE ADDITIVE ONLY, zero writes, zero migrations): `services/ledger.py::compute_contractor_ledger` now returns two new fields — `total_settled` = Σ settlements with `kind='contractor_settle'` for this contractor (filtered by window if start/end), and `final_balance = -net_paid` (same formula in current and history mode, deliberately NOT copying the worker formula because contractor settlements already record a matching return so their cash effect is in `total_returned`). Settlements list is also surfaced. Kind filter (`kind='contractor_settle'`) prevents contractor settlements from ever leaking into worker `total_settled`. `ContractorsSection.jsx` adds a direction line ("You owe contractor / Contractor owes you / Balanced"). `ContractorHistory.jsx` adds Total Settled KPI, direction card with red/green tone, and an explanatory footnote. Reports (`reports.py`): PDF adds Balance column + direction sentence, Excel Summary adds Total Settled/Net Paid/Balance/Direction rows, WhatsApp includes balance line. 7 new tests (40-46) cover: contractor lifetime net_paid, settle zeros balance + surfaces total_settled, full-year history shows settle, partial-return no-settle, user isolation, contractor settle never leaks into worker totals, dashboard pending_list direction. **47/47 tests pass.** Live HTTP smoke (`tests/smoke_contractor_reports.py`) confirms /ledger, PDF (with rendered text extracted via pypdf), Excel Summary sheet, WhatsApp all contain the new fields and correct direction sentence before AND after Mark Settled. No DB writes, no migrations, no worker/settlement logic touched.
- **[2026-02-14] Attendance: explicit Save flow + half-day manual wage + overtime manual amount:** Three approved fixes implemented additively without touching contractor accounting, settlement logic, or any existing DB records. **(1) Explicit Save flow** — `frontend/src/pages/Attendance.jsx` rewritten with per-row draft state (`drafts` dict), server snapshot (`server` dict), and dirty-tracking (`isDirty` helper). Status buttons and all inputs (manual wage, overtime amount, crop, description) now mutate ONLY local draft — no POST fires. Explicit Save button per row triggers a single `POST /api/attendance` gated by `saving` map (double-click safe). Cancel restores server snapshot. Failed saves keep the draft intact. Subtle "Unsaved" pill appears when dirty. **(2) Half-day manual wage** — new optional field `manual_wage: Optional[float]` on `Attendance` + `AttendanceIn` (backend routes/attendance.py). When status=half_day and manual_wage is present, ledger uses it verbatim; when absent, falls back to `0.5 × daily_rate_snapshot` for legacy compat. Server-side validation: non-negative, capped at max(100000, 10×daily_rate). **(3) Overtime manual amount** — new optional field `overtime_amount: Optional[float]`. When present: `earning = daily_rate_snapshot + overtime_amount`, `days_worked += 1` (no fractional inflation). When absent but overtime_hours>0: legacy `1 + hours/8` formula preserved verbatim. `_wage_units(status, ot_hours, rate, *, manual_wage=None, overtime_amount=None)` — kwargs keep signature backward compatible. `compute_worker_ledger` and both dashboard call sites updated to pass the new kwargs. 9 new tests (48–56) cover: manual wage verbatim, no-manual fallback, wage-edit-doesn't-rewrite-manual, overtime amount verbatim, wage-edit-doesn't-rewrite-overtime, legacy overtime_hours unchanged, PDF/Excel inheritance, full-year history with manuals, and live HTTP e2e (POST + UPDATE + validation rejects negative + ledger total ₹1,150). **56/56 tests pass.** Frontend `yarn build` clean. Backend `/api/health` ok. **No DB writes, no migrations, no legacy rows modified.**
- **[2026-02-14] Accounting Freeze — 5 defensive isolation fixes:** Applied the 5 approved additive ownership/response-consistency fixes discovered during strict checkpoint verification. **(1)** `routes/reports.py::report_pdf` and `report_excel` — 404 when a supplied `worker_id` is not owned by the caller (was returning 200 with empty PDF/Excel). **(2)** `routes/contractors.py::add_visit` — 404 when contractor_id is not owned (was inserting orphan row). **(3)** `routes/contractors.py::add_cpayment` — same 404 guard. **(4)** `routes/contractors.py::add_creturn` — same 404 guard. **(5)** `routes/workers.py::delete_worker` — checks `deleted_count == 0` and raises 404 (was returning 200 for cross-account delete attempts). All fixes are purely defensive: correct owner-side flows return 200; only non-owner accesses are refused. Zero DB writes. Zero legacy data changes. Zero settlement/ledger math changes. Added tests 57–62: worker report ownership, contractor visit/payment/return ownership + no stray rows verified via direct DB check, worker DELETE 404 with A's data intact, and a full owner-flow smoke that exercises worker+contractor+attendance+advance+return+contractor-visit+payment+return+PDF to prove the 404 guards didn't break happy path. **62/62 backend tests pass; frontend build clean; `/api/health` ok; contractor report smoke pass; final freeze verification 16/16 cross-account attempts blocked (up from 10/16 before the fixes).**

- **[2026-02-14] Phase 1 — Auth overhaul: Google-only + District removal + OTP-user migration:** Login flow narrowed to Google-only. Removed `POST /auth/otp/send`, `POST /auth/otp/verify`, `POST /auth/firebase/verify` (with helpers `_get_firebase_certs`/`_firebase_certs_cache`) and their Pydantic models from `backend/routes/auth.py`. Removed `frontend/src/pages/OtpLogin.jsx`; `App.js` `/otp` route dropped; `Login.jsx` "Continue with mobile" button removed. `Protected` gate now redirects to `/profile-setup` whenever `!(user.name && user.mobile)` regardless of auth provider. `ProfileSetup.jsx` rewritten: name + mobile (fixed `+91 |` prefix, exactly 10-digit numeric input, save-button disabled until valid). `Settings.jsx` edit-profile dialog: district field + row removed, mobile now shown with `+91 |` prefix.
  **Migration (Section 2 of spec)** — before removing OTP endpoints, verified 82 OTP-only users had data. New `_current_shell_has_data()` + inline merge logic in `POST /auth/profile`: when the current Google shell has no data yet AND the entered mobile matches a pre-existing OTP-only user (email empty), the OTP-only `user_id` is preserved (all worker/attendance/advance/return/settlement rows stay linked), the Google identity (`email`, `picture`, `google_linked_at`, chosen `name`) is added to the old user record, the current session_token is reassigned to that older `user_id` via cookie OR `Authorization: Bearer` header, and the empty Google shell is deleted. Real conflict (mobile owned by another Google user, or shell already owns data) returns HTTP 409. `POST /auth/profile` still accepts `district` (now Optional) for backwards compatibility so legacy test suites (`test_iteration3/4/5/6/7/8/9/10`, `backend_test.py`) keep passing unchanged. `/districts` endpoint retained (Owner Portal ad targeting still uses it). Downstream district readers verified safe: `routes/ads.py` already falls back to global ads when district is empty, `routes/owner.py` marks missing districts as "Uncategorized".
  **Test evidence:** 6/6 new `tests/test_auth_migration.py` pass (merge-happy-path, session reassignment, 409 on real conflict, shell-has-data guard, district-optional, mobile-length validation, empty-name rejection). Regression: 62/62 accounting, 7/7 FREE_MODE, 5/5 calendar isolation, 1/1 present-vs-days — all green. Frontend build clean (`yarn build` 14.95s).
  **Browser E2E** (4 mandatory + 3 supporting screenshots): 01 Google-only login (0 OTP buttons), 02 first-login setup with +91 prefix + 10-digit input, 03a/b Settings edit-profile pre-filled + successful save, 04a/b/c Legacy Farm Owner (OTP-only) → Google-login shell → mobile 9448400400 entered → toast "Linked to your existing account" → Dashboard shows "Legacy's Farm" heading + "Migrant Worker Alpha" (the pre-existing legacy worker) visible in calendar dropdown. **Zero accounting/contractor/worker/attendance/history/PDF/settlement/advance/return code touched.** Git checkpoints: `pre-auth-overhaul` (d036a49) → `post-auth-overhaul` (132f30f).

- **[2026-02-14] Phase 2 — Dashboard redesign + Attendance Calendar (UI + new read-only backend):** Dashboard trimmed and refocused around actual daily activity. Removed 3 stat cards (Workers total, Today's Wage, Pending Wage) from `Dashboard.jsx`; kept Present Today. Added new touch-first `AttendanceCalendar.jsx` component: worker selector (auto-selects earliest-created worker), month header with prev/next navigation, 7-column grid, dates highlighted only when the selected worker has a real `present/half_day/overtime` record (absent rows never highlight). Tapping ANY date opens a bottom sheet listing ALL of the caller's workers with a worked record on that date (never inferred — status pass-through from the DB row). Empty state ("No workers yet" selector + empty grid) when the user has zero workers. Added "Agri Expenses — Coming Soon" visual placeholder card. New READ-ONLY backend module `routes/calendar.py` (2 endpoints): `GET /api/calendar/month?worker_id=&year=&month=` returns highlighted dates for one worker, `GET /api/calendar/date?date=` returns all caller's workers with a worked record. Both check ownership (`worker.user_id == caller`), both omit absent rows, both are strictly read-only (no writes, no accounting math, no ledger/wage/settlement calls). Wired into `main.py`. New test suite `tests/test_calendar_isolation.py` (5/5 pass, isolated processes): worked-only records, foreign-worker-id rejected 404, date returns only caller's workers with same-date-cross-user proof, absent omitted, half_day/overtime/present pass-through no inference. Live 2-account browser E2E confirmed zero cross-user leakage. **Zero accounting changes** — 62/62 accounting regression tests pass, `services/ledger.py`, `routes/attendance.py`, `routes/reports.py`, `routes/advances.py`, `routes/settlements.py`, `routes/contractors.py`, `routes/dashboard.py` all untouched. Frontend build clean (`yarn build` Done in 15.11s). Git checkpoints: `pre-dashboard-calendar` (8350831) → `post-dashboard-calendar` (cf9fa4b).

- **[2026-02-14] Phase 3 follow-up — FREE_MODE server-side gate:** Backend `require_write_access` was still emitting HTTP 402 "Trial expired" for users past their 15-day trial, so the UI-only Phase 3 changes did not deliver a truly free app. Minimum-surface fix in `security/authorization.py`: added `_free_mode_enabled()` env helper (reads `FREE_MODE`, default `"1"`), and short-circuited `compute_access()` when free mode is on to always return `{is_paid: True, locked: False, subscription_active: True, subscription_days_left: 9999, free_mode: True}`. `require_write_access` is untouched — it still calls `compute_access` and only raises 402 when `locked=True`, so with FREE_MODE=1 no 402 can be produced. All payment routes (`routes/payments.py`), Razorpay handlers, and DB fields (`trial_start`, `is_paid`, `subscription_expires_at`) are preserved verbatim; setting `FREE_MODE=0` restores the historical behavior bit-for-bit. New test suite `tests/test_free_mode_access.py` (7/7 pass) covers: default free-mode on, expired-trial user unlocked, no-trial-start user unlocked, `require_write_access` doesn't raise for expired trial, gate re-enables when FREE_MODE=0 (locked=True), gate raises 402 when re-enabled, paid-lifetime user unaffected. Live HTTP verification against expired-trial user (trial_start 90 days ago): `/auth/me` → `locked=false, free_mode=true`; 11 write endpoints tested (workers, attendance, advances with method, returns, contractors, contractor-visits, contractor-payments, settlements, reports/pdf, ledger, /auth/me) — **zero HTTP 402s emitted**. Browser E2E across 6 screens (Dashboard, Attendance with live Save, Workers, Worker History, Reports, Settings) confirmed 0/402 responses. 62/62 accounting regression tests still pass, frontend build clean. **Zero accounting math changes.** Git checkpoints: `free-access-pre` (9b2aaba) → `free-access-post` (e9432cc).

- **[2026-02-14] Phase 3 — Free Model Pivot + Reports Rename (UI-only, non-accounting):** App transitioned to fully free. All promotional/trial/subscription/upgrade UI stripped: `TrialBanner.jsx` now returns null, Settings' "Upgrade" card and "Xd left / Lifetime" subscription badge removed, Login's "15-day free trial" & "PDF & Excel export" feature rows replaced with "Completely free · All features included" + "PDF reports". `locked = user?.access?.locked` replaced with `locked = false` inline in `Attendance.jsx`, `Workers.jsx`, `Ledger.jsx`, `ContractorsSection.jsx` (both components) so all write-actions are always unlocked. Excel export UI removed: `export-excel-btn` from Ledger reports header and `contractor-excel-btn` from Contractor detail sheet. Translation keys `export_excel` / `read_only_notice` / `read_only_desc` / `lifetime_price` cleaned from `i18n.js`. **User-facing "Ledger" → "Reports" rename**: nav label (`BottomNav` uses `t("ledger")` — dictionary value now "Reports" / "ವರದಿಗಳು"), AppShell title on `/ledger` page. Backend routes (`/api/ledger`, `/api/reports/excel`), imports (`services.ledger`, `compute_worker_ledger`), and variable names remain untouched. Razorpay backend routes and `/paywall` page intact but orphaned (no UI entry points). PDF header confirmed rendered as "Farm Labor Report" — no "Ledger" leakage. **Zero accounting changes.** Verified via screenshots: Dashboard, Reports, Workers, Contractor detail, Settings, Login, Worker History, and generated PDF all clean. Worker History "Present vs Days Worked" bug confirmed still fixed (Present=1, Days Worked=1 as separate KPIs).

 Applied the 5 approved additive ownership/response-consistency fixes discovered during strict checkpoint verification. **(1)** `routes/reports.py::report_pdf` and `report_excel` — 404 when a supplied `worker_id` is not owned by the caller (was returning 200 with empty PDF/Excel). **(2)** `routes/contractors.py::add_visit` — 404 when contractor_id is not owned (was inserting orphan row). **(3)** `routes/contractors.py::add_cpayment` — same 404 guard. **(4)** `routes/contractors.py::add_creturn` — same 404 guard. **(5)** `routes/workers.py::delete_worker` — checks `deleted_count == 0` and raises 404 (was returning 200 for cross-account delete attempts). All fixes are purely defensive: correct owner-side flows return 200; only non-owner accesses are refused. Zero DB writes. Zero legacy data changes. Zero settlement/ledger math changes. Added tests 57–62: worker report ownership, contractor visit/payment/return ownership + no stray rows verified via direct DB check, worker DELETE 404 with A's data intact, and a full owner-flow smoke that exercises worker+contractor+attendance+advance+return+contractor-visit+payment+return+PDF to prove the 404 guards didn't break happy path. **62/62 backend tests pass; frontend build clean; `/api/health` ok; contractor report smoke pass; final freeze verification 16/16 cross-account attempts blocked (up from 10/16 before the fixes).**




    1. `_compute_pending_list` (services/ledger.py) returned `pending = total_earned` — Dashboard marquee was misleading. Fixed to use `final_balance` (single source of truth). Also corrected contractor `advance` field to use `net_paid` and marked contractor pending as negative for sign-consistency with worker marquee items.
    2. `/api/dashboard` (routes/dashboard.py) computed `pending_wage = total_earned − total_advance` (all-time, ignoring returns and settlements) and `outstanding_advance = total_advance` (ignoring returns). Rewrote so both totals aggregate the per-worker ledger — `pending_wage = Σ max(0, w.final_balance)`, `outstanding_advance = Σ max(0, w.net_advance)` — guaranteeing dashboard equals sum of individual Ledger cards.
    3. `POST /api/advances`, `POST /api/returns`, `POST /api/attendance` accepted arbitrary `worker_id` without confirming the worker belongs to the caller. Added `db.workers.find_one({"id": worker_id, "user_id": caller})` guard on each — cross-account writes now 404. Verified via live HTTP test.
    4. Extended `/app/backend/tests/test_accounting_audit.py` to 33 automated scenarios covering multi-worker isolation, advance-after-settle (K), return-before-settle (L), absent+advance same day (R/S), half_day + overtime wage math, dashboard=Σworkers, dashboard pending_list scoping, cross-account POST rejection (28, 29), PDF/Excel==UI single source, three-cycle settle→balanced, and reload stability. 33/33 pass.
  Verified live via authenticated HTTP round-trip against the deployed API: dashboard aggregate matches per-worker sum; cross-account /advances, /returns, /attendance all return 404.

- **[2026-02-13] Balance color/direction fix:** `Stat` helper in `Ledger.jsx` now supports a `danger` prop; the worker-card Balance cell uses `primary` (green) when `final_balance > 0`, `danger` (red) when `< 0`, and neutral when `= 0`.
- **[2026-02-13] Full accounting audit & period-aware Final Balance (v2):** Fixed the post-settlement double-counting bug uncovered by the user (earned 1950 + adv 2500 + settle 1950 was showing "Worker owes ₹4,450" instead of ₹2,500). `compute_worker_ledger` in `services/ledger.py` now branches:
    - CURRENT-cycle mode (no start/end): `final_balance = total_earned − net_advance` — does NOT subtract `total_settled` because those earnings were already reset by cutoff.
    - HISTORY mode (start & end supplied): `final_balance = total_earned − net_advance − total_settled` — all three values are windowed to the same date range, so classical activity accounting applies.
  Backend is the single source of truth: `Ledger.jsx`, `WorkerHistory.jsx`, PDF, Excel, WhatsApp all consume `led['final_balance']`. Reports now also include a "Balance" column and WhatsApp messages now say "You owe worker" / "Worker owes you" / "Balanced" instead of the misleading "Pending". Comprehensive audit suite `/app/backend/tests/test_accounting_audit.py` covers all 21 required scenarios (earned-only, advance-only, mixed, exact settle, actual_paid>earned, adjust_advance both directions, multi-advances/returns/settlements, undo settle, full-year/month/pre-settle history, PDF/Excel consistency, cross-user isolation, Ledger==WorkerHistory) — 21/21 pass. Old test files `test_final_balance.py` and `test_settlement_final_balance.py` removed (replaced by the audit suite).



## Backlog (P1/P2)
- **P1** Real SMS OTP (Firebase Phone Auth once user upgrades to Blaze, or MSG91/Twilio fallback)
- **P1** Razorpay Webhook Configuration (RAZORPAY_WEBHOOK_SECRET) for out-of-band capture
- **P2** WhatsApp renewal nudges 7 days before subscription expiry
- **P2** Multi-admin management UI in Owner Portal (promote/demote without editing .env)
- **P2** Weekly WhatsApp summary of attendance/wages to owner
- **P2** iOS `apple-touch-startup-image` splash screens for device-specific PWA launch

## RBAC / Monetization Rules
- `is_owner` granted when logged-in mobile == OWNER_MOBILE or email == OWNER_EMAIL (backend/.env)
- Trial: 15 days from `created_at`. After that, writes return 402 unless `subscription_active` and `subscription_expires_at > now`
- Razorpay success grants +365 days

## Key Files
- `/app/backend/server.py` — API, RBAC, ledger cutoff, PDF/Excel, Razorpay
- `/app/frontend/public/manifest.json` + `/app/frontend/public/index.html` — PWA config
- `/app/frontend/public/icons/*` — all icon sizes + maskable variants
- `/app/frontend/src/pages/*` — Dashboard, Ledger, Workers, Settings, OwnerPortal, Paywall, OtpLogin, Login
